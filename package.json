{
  "name": "aiilp-root",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w ./backend\" \"npm run dev -w ./frontend\"",
    "build": "npm run build --prefix frontend && cp -r frontend/dist ./dist",
    "vercel-build": "npm run build --prefix frontend && mkdir -p dist && cp -rv frontend/dist/* dist/",
    "install-all": "npm install"
  },
  "dependencies": {
    "vite": "^4.5.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  }
}