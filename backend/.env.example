# Backend environment variables

# Supabase service role key is REQUIRED for admin endpoints
# Find it in Supabase Dashboard → Project Settings → API → Service Role Key
SUPABASE_SERVICE_ROLE_KEY=

# Optional: override port (default 3001)
# PORT=3001

# Optional: set Supabase URL and anon key (otherwise falls back to frontend values)
# SUPABASE_URL=https://YOUR-PROJECT.supabase.co
# SUPABASE_ANON_KEY=YOUR_ANON_KEY